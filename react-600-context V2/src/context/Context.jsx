import { createContext } from "react";
//createContext() 함수를 실행하여
//실행결과를 export
//reac.createContext()를 싱행하면 context.provider가 export된다
//이제부터 Context.jsx는 state변수들, 여러가지 함수들을
//보관했다가 요청하는 곳에 공급하는 store 역할이 된다.
export default createContext();
